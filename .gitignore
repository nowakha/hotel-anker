# OS generated
.DS_Store
Thumbs.db
Desktop.ini

# Editors / IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Local env overrides (credentials live under WerbeLEDbox-CountDown/secrets/ by project request)
.env
.env.*
!.env.example

# Dependencies / build
node_modules/
dist/
build/
out/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Python bytecode / local venvs only (NOT project data)
__pycache__/
*.py[cod]
.venv/
venv/
WerbeLEDbox-CountDown/.venv-opencv/

# PlatformIO build cache (source + platformio.ini stay tracked)
**/.pio/

# POLICY 2026-07-27 (Harald): Project deliverables, print PDFs, ZIPs, photos,
# media, secrets handoff files MUST be tracked. Do not re-add ignore rules that
# hide Anker/Richnerstutz/print/frame assets "because they are large".
# GitHub soft limit ~100 MB/file — use Git LFS only if a single file exceeds that.
# Encode scratch partials only:
WerbeLEDbox-CountDown/media/_encode*
WerbeLEDbox-CountDown/media/_scp*
WerbeLEDbox-CountDown/fonts/_extract/
WerbeLEDbox-CountDown/docs/_pi02_rescue.log

# print build cache
Richnerstutz-Bespannung-Paket/02-druckdaten/_trim_cache/

# local gmail send screenshots
Richnerstutz-Bespannung-Paket/versand/_gmail_*.png
