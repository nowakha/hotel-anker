# AnkerPI01 credentials
# WARNING: Secrets are stored in this repository by project request (private handoff).
# Do not publish this repo publicly without removing or rotating these values.

host: AnkerPI01.local
ip: 192.168.8.102
ssh_user: user
ssh_password: "12345678"
tailscale_hostname: AnkerPI01
tailscale_ip: pending_auth
tailscale: installed_1.98.9_needs_login
dns: "1.1.1.1 8.8.8.8 (NM ignore-auto-dns=yes; verified 2026-07-22 ~18:12)"
wifi_secret: secrets/wifi.hotelanker.yml
notes: >
  DHCP jumps (seen .102 and .108) — prefer AnkerPI01.local.
  Password login remains enabled.
  SSH keys authorized: hotel-anker-dev@TABLETHI10MAX (legacy),
  hotel-anker-dev@MLT-NITRO5-HN (2026-07-22).
  WiFi (2026-07-22 ~18:12): persistent NM keyfile
  /etc/NetworkManager/system-connections/HotelAnker.nmconnection
  (autoconnect-priority 20, powersave=2, DNS 1.1.1.1/8.8.8.8).
  Active: wlan0 → HotelAnker (2.4 GHz @ 2432 MHz) → 192.168.8.102.
  HotelAnker_5G NOT created — Pi Zero 2 W radio is Band 1 only (no 5 GHz);
  SSID never visible in scan. netplan-wlan0-HotelAnker remains in /run as
  legacy fallback (priority 0). Also: systemd wlan-powersave-off.service.
  apt: /etc/apt/apt.conf.d/99force-ipv4 Acquire::ForceIPv4 true.
  Tailscale 1.98.9 arm64 installed 2026-07-22 ~18:05 via LAN SCP of
  pkgs.tailscale.com/stable/debian/pool/tailscale_1.98.9_arm64.deb
  (NOT apt/CloudFront on Pi WiFi). tailscaled enabled/active.
  Join: sudo tailscale up --hostname=AnkerPI01 --accept-dns=false
  Auth URL (NeedsLogin): https://login.tailscale.com/a/144cabd401ab72
  After user authenticates, fill tailscale_ip and set tailscale: joined.
